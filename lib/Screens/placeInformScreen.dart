import 'package:flutter/material.dart';

class placeInformScreen extends StatefulWidget {
  const placeInformScreen({Key? key}) : super(key: key);

  @override
  State<placeInformScreen> createState() => _placeInformScreenState();
}

class _placeInformScreenState extends State<placeInformScreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(

      ),
    );
  }
}
